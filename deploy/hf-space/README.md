---
title: Soft Sensor Toolbox API
emoji: 🧪
colorFrom: blue
colorTo: green
sdk: docker
app_port: 7860
pinned: false
---

# Soft Sensor Toolbox API

FastAPI backend of the Soft Sensor Toolbox. The web app is served separately
from GitHub Pages; this Space only answers its API calls.

This repository is generated by the main project's `deploy-backend` GitHub
Action on every push. Edit the main repository, not this one.
